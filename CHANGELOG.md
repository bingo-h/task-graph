# 更新日志

本文件记录 task-graph 各版本的主要变更。格式参考 [Keep a Changelog](https://keepachangelog.com/)，但用中文撰写。

> **打 tag 发布前**：把下面的 `[未发布]` 改成 `[X.Y.Z] - YYYY-MM-DD`（版本号跟即将打的 tag 一致，不带 `v` 前缀），再在它上面新开一个空的 `[未发布]` 供后续开发使用。CI（`.github/workflows/release.yml`）会按 tag 版本号从这里切出对应章节放进 GitHub Release 说明里，章节没改名会导致 Release 说明里显示"未发布"的内容或直接为空。

## [未发布]

### 修复

- 日历页日视图这一天没有任何计时记录时，原来整个 0-24 点时间轴都不显示、只有一行文字提示；现在时间轴刻度始终完整显示一整天，不会因为没数据就整个空掉。
- "今日任务"标记、重复任务的每日/每周/每月周期截止时间，过了当地时间 0 点后不会自动翻篇：这些判断原来都用 UTC 日期而不是本地日期，时区偏移较大的用户会在本地已经跨天后还要再等几个小时（等 UTC 也跨天）才会翻篇——比如重复任务前一天下午完成后，第二天本地已经是新的一天，但只要 UTC 还没跨天，任务仍会显示为"已完成"而不是重置回待办。另外应用没有任何定时刷新机制，即使跨天时刻已过，只要没做任何触发数据刷新的操作界面也不会更新。现在"今日任务"标记和重复任务的周期截止时间都改用本地日期计算，任务看板也和首页/图表页一样加了 5 分钟定时兜底刷新。

## [1.2.4] - 2026-08-17

### 新增

- **多选任务批量转移项目**：任务看板框选/Ctrl 多选一批任务后，批量操作工具栏新增"移动到项目"按钮，可以把选中的任务一次性转移到同一个项目下（或转移到"无项目"）。
- **自绘确认弹窗**：删除项目/标签/任务、批量删除、删除计时记录这几处原来弹的是浏览器原生 `confirm()`，样式跟应用完全不搭；现在换成和其它弹窗风格一致的自绘确认框，危险操作的确认按钮标红。
- **首页/分析页跳转任务时自动定位项目**：在首页"今日任务"、图表页、日历页点击某个任务跳转到任务看板时，除了选中这个任务，看板的项目筛选也会自动切到它所属的项目（没有项目则切到"无项目"分类），直接显示它所在的流程图，不用再手动去项目树里点一次。
- **任务详情显示完成时间**：任务完成后，详情面板新增"完成于"一行，展示具体完成日期和时间（数据库其实早就记录了这个时间戳，之前只是没有在界面上显示出来）。
- **图标 / 颜色改成点选**：任务表单里的图标从手动输入 emoji 改成点选（预置一批常用打卡图标 + "清除图标"），颜色选择器除了固定色板，新增了饱和度/明度取色盘 + 色相滑条，可以自由取色，不用再猜 hex。
- **界面字体设置**：设置里新增"字体"（通用分区），会读取系统已安装的字体列表，输入关键字即可模糊搜索筛选，选中后立即预览效果；后端用纯 Rust 的 fontdb 扫描字体目录，不依赖系统原生字体 API。默认字体从小众等宽字体 Maple Mono NF 换成了通用无衬线字体（`sans-serif`，跟着系统默认走）。
- **任务节点单独字体**：图谱显示分区新增"节点字体"，可以单独给任务看板里的任务卡片文字设一个跟全局不一样的字体；留空则跟随上面的全局字体设置，改了之后再清空也会自动恢复跟随。

### 修复

- 任务表单"前置任务"候选列表原来完全排除已完成的任务，导致没法给"实际上已经做过"的任务补一条依赖记录；现在同一项目下的已完成任务也会作为候选出现（标"✓ 已完成"徽章区分），未选择项目时行为不变。
- **修复选取颜色时应用崩溃的问题**：任务表单和标签管理里的颜色选择器原来用的是系统原生 `<input type="color">`，在部分 Linux 发行版（尤其 NixOS，打包环境缺少 GTK 的 GSettings schema 时）弹出瞬间会直接让整个应用崩溃退出。现在换成纯前端实现的取色面板，不再依赖系统对话框，各平台表现也更一致。
- 任务详情"完成于"这一行原来直接截取后端存的 UTC 时间字符串显示，没有换算成本地时区，导致显示的钟点和用户本地实际完成的时间对不上（例如本地 UTC+10 时区会显示成少 10 小时）；现在会正确换算成浏览器所在时区再显示。
- 全面排查了一遍其它涉及时间显示的地方，修了三处类似的时区问题：首页"即将到期"列表的日期原来经过 `Date` 对象换算成本地时区，导致正偏移时区（如 UTC+8/+10）下显示的日期比实际截止日期晚一天；日历页日视图的时间轴原来按 UTC 的一天划分、时刻按 UTC 显示，导致同一天的计时记录可能被划错日期、时刻也对不上本地钟点；任务详情里计时记录按日期分组的标题原来直接取 UTC 日期，和组内展示的本地时刻不一致。

### 变更

- 框选/Ctrl 多选后，左键点击任意任务节点或画布空白处（不按住 Ctrl）会直接取消当前多选，不用再专门点"取消选择"按钮，批量操作工具栏里也移除了这个按钮。
- 任务节点卡片的任务描述文字改为加粗、字号加大，和下面几行详情信息的字号拉开差距，标题和详情更容易一眼区分。

## [1.2.3] - 2026-08-17

### 新增

- **任务看板图谱多选与批量操作**：Ctrl/Cmd + 点击可多选任务节点；按住右键长按拖拽可框选一批任务。选中后弹出批量操作工具栏，支持批量完成、删除、设为今日任务、开始计时。
- **批量计时**：框选/多选一批任务后可以一起开始计时，共享同一段开始时间、各自单独记一条计时记录；停止计时只结束这段时间，不会连带把任务标记为完成。标题栏悬浮秒表在批量计时时显示"正在为 N 个任务计时"，鼠标悬浮可展开具体是哪些任务。
- **拖拽建立任务依赖关系**：鼠标靠近任务节点边框时出现连接点，按住拖到另一个节点上可以建立依赖——拖右侧点表示这个任务是目标任务的前置任务，拖左侧点表示是后置任务。新增边之前会做环检测，避免出现循环依赖。
- **拖拽已有依赖连线的终点**：抓住某条依赖线的终点（箭头处）拖到空白处会删除这条依赖，拖到另一个任务上则把依赖关系改指向那个任务。
- **标签独立数据模型**：标签从原来存在每个任务上的 JSON 数组，改成独立的 `tags` 表（名字 + 颜色）与 `task_tags` 多对多关联表。旧数据库打开时会自动迁移，无需手动操作。
  - 标签支持设置颜色，任务详情、任务表单里的标签徽章会跟着显示对应颜色。
  - 新增"标签管理"面板（标题栏 🏷 按钮）：列出所有标签，可改名（用了这个标签的任务一起改名，若改成已存在的标签名则自动合并）、改颜色、删除，并显示每个标签被多少个任务使用。
  - 点击任意位置的标签徽章可以跳转到任务看板并按这个标签筛选图谱，可与项目筛选同时生效。
- **标签 / 项目下拉框键盘导航**：输入标签、选择所属项目时，下拉建议框支持 ↑↓ 移动高亮、Enter 确认选择，鼠标悬浮也会同步高亮；顺带优化了下拉框的视觉样式（圆角、阴影、进场动画）。
- **标签下拉框内联改名**：鼠标悬浮某个已有标签时会出现 ✎ 图标，点击后原地变成输入框，Enter 确认后所有用到这个标签的任务会一起改名。
- **DAG 视图同层任务手动纵向排序**：右键框选或 Ctrl/Cmd + 右键点选出一批没有依赖关系、处于同一层级的任务后，左键按住其中一个已选中的节点即可整体拖拽，调整它们在纵轴上的先后顺序；松手后顺序会持久化保存，重新打开应用/切换视图后依然保持。这个顺序独立于真实依赖关系和"今日任务"排序，只影响同层任务的展示顺序。
- **任务节点卡片信息可配置显示 + 悬浮详情窗**：项目、截止日期、优先级、重复任务标记改成各占一行（`项目：xxx`、`截止时间：2026-08-17` 格式），不再挤在一起。设置里新增"图谱显示"分区：
  - 可以分别开关卡片上显示这四项信息；开启的项即使任务没有对应的值也会显示（标为"无"），不会因为个别任务缺字段就错位。
  - 每一项的标签文字（"项目""截止时间""优先级""重复"）都可以自定义，最多 8 个字符；旁边的重置图标按钮固定占位，不会因为出现/消失导致输入框跟着变宽变窄，标签改成默认值时按钮会置灰。
  - 节点卡片高度按开启的信息项数自动调整，开得越多卡片越高，不再是固定高度。
    鼠标悬浮在任务卡片上会弹出一个详情窗，不受上面这些显示开关影响，始终展示状态、项目、优先级、截止日期、计划开始、重复规则、前置任务数、累计耗时、紧迫度、标签等完整信息。

### 修复

- 项目树拖拽排序/移动过于敏感的问题：`mousedown` 后需要移动超过一定距离才会真正触发拖拽状态，避免单纯点击也会闪现一次拖拽提示牌。
- 任务节点描述过长时文字会超出节点边框：原来按固定字符数截断，在中文等宽字体下仍可能溢出，改为按实际渲染宽度动态截断。
- 首页/图表页的专注时长统计跨天后不显示数据：这两个页面用 `v-show` 常驻挂载，计时记录只在组件第一次挂载时拉取一次，之后无论是跨天还是在别处新增了计时记录都不会自动刷新（这也是为什么重启应用能"修好"）。现在切回这两个页面时会自动刷新一次，并额外加了 5 分钟定时兜底刷新。
- 首页"今日任务"为空时的提示文案还指向已经不存在的"设为今日任务"按钮，改成指向任务详情标题旁的 ☀ 图标。
- 任务表单里"前置任务"候选列表原来会跨项目显示所有待办任务，现在会按当前选择的项目过滤（未选择项目时仍显示全部）；标签建议同理。
- 拖拽依赖连线终点时，抓取识别的位置和线条实际画出来的落点对不上：一个任务被多个任务依赖（有多条入边）时，dagre 会把这些边在节点左边框上错开分布而不是全部挤在正中间，但拖拽手柄之前固定写死在节点左边框正中间，导致抓不准。现在手柄改成取和连线本身同一份坐标数据的终点，两者保证一致。
- "今日任务"视图里手动排序边（绿色）的箭头颜色和线条颜色对不上：箭头之前全局共用一个写死灰色的 marker，现在按边的类型（真实依赖/今日排序/依赖链路虚线预览）各自用对应颜色的箭头。
- "今日任务"视图悬浮/选中节点时预览的原依赖链路虚线，之前完全没有画箭头，看不出链路方向；现在虚线也带上了（洋红色）箭头。
- 任务悬浮详情窗里截止日期那一行同时有"截止"标签和"Due:"前缀，重复了；去掉多余的"Due:"。

### 变更

- 首页仪表盘"完成任务"卡片格式改为"已完成 / 全部任务"，展示总体完成度，不再随时间范围选择器变化；已归档、废纸篓中的项目不再计入首页各项汇总统计。
- 首页仪表盘"时间占比"改名为"专注时间占比"，含义不变（仍随天/周/月范围选择器变化）。
- 标题栏布局调整：悬浮秒表移到标题栏正中间；"添加任务""刷新""标签""设置"等按钮统一挪到最右侧、紧贴窗口控制按钮左边。
- 选中任务、高亮依赖链路时，不再淡化依赖连线本身，只淡化不相关的节点。
